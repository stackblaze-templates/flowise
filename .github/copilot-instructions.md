# Copilot Instructions

This is a Flowise (low-code LLM app builder) template for StackBlaze.

## When checking for updates
- Check if flowiseai/flowise Docker image has newer stable tags
- Verify docker-compose.yml and Dockerfile reference current images
- Check for new required environment variables in newer versions
- Keep the template minimal — deployment-ready with local dev support